# Execute one Target per Run

Presets and Manual Targets normalize to the same Target contract, and every Run accepts exactly one Target. A future Batch expands into sequential independent Runs so targets share one executor and do not compete for client bandwidth while being compared.
